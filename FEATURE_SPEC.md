# 📚 Reference Helper 기능 명세서

## 1. 프로젝트 개요
**Reference Helper**는 기술 블로그 작성 시 참고 자료(URL, 제목, 인용구)를 효율적으로 수집, 관리하고 에디터에 자동으로 삽입해주는 Chrome 확장 프로그램입니다.

---

## 2. 핵심 기능 상세

### 2.1. 참고 자료 수집 (Capture)
웹 서핑 중 다양한 방법으로 자료를 수집할 수 있습니다.

| 방법 | 동작 방식 | 특징 |
|------|----------|------|
| **우클릭 메뉴** | 페이지/링크/텍스트 우클릭 → `Reference Helper` → `[목록명]에 추가` | 특정 목록을 선택하여 추가 가능 |
| **키보드 단축키** | `Ctrl/Cmd + Shift + E` | 현재 페이지를 **기본 목록(⭐)**에 즉시 추가 |
| **팝업 버튼** | 팝업 UI 내 **`📌 현재 페이지 추가`** 버튼 | 현재 보고 있는 페이지를 현재 선택된 목록에 추가 |
| **복사 감지** | 텍스트 복사(`Ctrl+C`) 시 자동 감지 | **복사 기록** 탭에 자동 저장 (설정에서 ON/OFF 가능) |
| **방문 감지** | 특정 블로그(Tistory 등) 방문 시 20초 후 감지 | **방문 기록** 탭에 자동 저장 (설정에서 ON/OFF 가능) |

> **✨ 중복 방지 시스템**: 모든 추가 경로에서 URL 중복을 검사합니다. 이미 존재하는 경우 추가하는 대신 "⚠️ 이미 [목록명]에 있는 페이지입니다" 알림을 표시합니다.

### 2.2. 목록 관리 (List Management)
수집한 자료를 체계적으로 관리합니다.

- **다중 목록 지원**: 주제별로 무제한 목록 생성 가능 (`+` 버튼)
- **기본 목록 설정**: 자주 쓰는 목록을 **기본 목록(⭐)**으로 지정 (단축키 추가 시 사용됨)
- **항목 편집**:
  - **순서 변경**: `↑`, `↓` 버튼으로 항목 순서 조정
  - **이동**: 다른 목록으로 항목 이동
  - **삭제**: 개별 항목 또는 목록 전체 삭제
  - **일괄 삭제**: 체크박스로 여러 항목 선택 후 일괄 삭제 (내 목록/방문 기록/복사 기록 모두 지원)
- **검색**: 제목, URL, 선택된 텍스트 내용을 실시간 검색

### 2.3. 블로그 작성 지원 (Writing Support)
모든 웹사이트의 에디터에서 참고 목록을 삽입할 수 있습니다.

- **범용 에디터 지원 (Universal Support)**:
  - **모든 웹사이트**의 입력창(Textarea, ContentEditable, CodeMirror 등)에서 작동
  - 단축키 `Ctrl/Cmd + Shift + L`로 즉시 삽입

- **자동 팝업 지원 플랫폼 (Optimized Platforms)**:
  - 다음 플랫폼에서는 **글쓰기 페이지 자동 감지** 및 **팝업 자동 열기** 기능이 추가로 지원됩니다.
  - ✅ **Tistory** (관리자/구형/신형 에디터)
  - ✅ **Velog** (마크다운 에디터)
  - ✅ **Medium** (ContentEditable)
  - ✅ **Brunch** (ContentEditable)

### 2.4. 내보내기 및 삽입 (Export & Insert)
수집한 목록을 글 양식에 맞춰 변환합니다.

- **Markdown 복사**: `[제목](URL)` 형식으로 변환하여 클립보드 복사
- **HTML 복사**: `<li><a href="...">제목</a></li>` 형식으로 변환
- **자동 붙여넣기**:
  - 팝업 하단 버튼 클릭 시 클립보드에 복사
  - 단축키 `Ctrl/Cmd + Shift + L` 입력 시 **에디터에 즉시 삽입**
  - 목록이 비어있을 경우 "⚠️ 목록이 비어있습니다" 알림 표시

---

## 3. 사용자 인터페이스 (UI)

### 3.1. 메인 팝업
- **헤더**: 목록 선택, 기본 목록 설정(⭐), 새 목록(+), 목록 삭제(🗑️)
- **검색창**: 실시간 필터링 지원
- **탭 메뉴**:
  - **내 목록**: 사용자가 직접 추가한 자료
  - **방문 기록**: 자동으로 수집된 방문 이력 (최근 100개, 보관 기간 설정 가능)
  - **복사 기록**: 클립보드 복사 이력 (최근 100개, 중복 시 최상단 이동)
- **푸터**:
  - **`📌 현재 페이지 추가`** (파란색 강조)
  - `📝 Markdown 복사` / `🌐 HTML 복사`

### 3.2. 설정 페이지 (Options)
- **일반 설정**: 복사/방문 추적 ON/OFF, 팝업 자동 열기 ON/OFF, 자동 번호 매기기
- **방문 추적 설정**: 보관 기간(1~90일), 제외 도메인 설정
- **템플릿 커스텀**: Markdown/HTML 변환 형식을 자유롭게 편집 (`{{title}}`, `{{url}}` 등 변수 사용)
- **데이터 관리**: 전체 데이터 백업(JSON 내보내기) 및 복원, 초기화

---

## 4. 단축키 (Shortcuts)

| 단축키 (Mac/Win) | 기능 |
|----------------|------|
| `Ctrl/Cmd + Shift + E` | **현재 페이지 추가** (기본 목록으로) |
| `Ctrl/Cmd + Shift + L` | **참고 목록 붙여넣기** (에디터 커서 위치에) |
| `Ctrl/Cmd + Shift + P` | **팝업 열기** |

---

## 5. 기술적 특징
- **데이터 동기화**: Chrome Storage Sync를 사용하여 동일한 구글 계정의 모든 기기에서 데이터 동기화
- **범용성**: 특정 플랫폼에 종속되지 않고 모든 웹사이트에서 작동하도록 설계
- **사용자 피드백**: 모든 작업(성공, 중복, 실패)에 대해 명확한 **Toast 알림** 제공
